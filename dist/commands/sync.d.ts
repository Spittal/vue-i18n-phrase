import { SyncCommandOptions } from '../types';
export declare function sync({ vueFiles, accessToken, project, tags, makeTranslation, skipReport, dryRun, outputDir, }: SyncCommandOptions): Promise<void>;
