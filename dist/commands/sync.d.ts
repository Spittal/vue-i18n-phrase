import { SyncCommandOptions } from '../types';
export declare function sync({ vueFiles, accessToken, projectID, tags, makeTranslation, skipReport, dryRun, outputDir, }: SyncCommandOptions): Promise<void>;
